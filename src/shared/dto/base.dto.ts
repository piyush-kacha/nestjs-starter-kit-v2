// src/shared/dto/base.dto.ts
export abstract class BaseDto {
  // Example properties that could be part of a BaseDto for response mapping:
  // id: string;
  // createdAt: Date;
  // updatedAt: Date;

  // For request DTOs, common properties are less frequent unless
  // there's a universal requirement like tenantId or trackingId.

  // For now, this serves as a placeholder for potential common fields
  // or utility methods that could be added later.
}
